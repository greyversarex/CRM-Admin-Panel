Прошлые отчёты были провальными. Ты перечислял названия разделов, но не разбирал
вкладки внутри них. А когда я просил разобрать конкретные — ты описал только те,
что я назвал, и пропустил остальные.

ТЕПЕРЬ ПРАВИЛА МЕНЯЮТСЯ. Я НЕ даю тебе список разделов или вкладок.
ТЫ САМ должен найти ВСЁ, что существует в проекте.

=====================================================
ГЛАВНАЯ ЗАДАЧА
Проведи ПОЛНЫЙ DISCOVERY всего проекта. Найди КАЖДУЮ страницу, КАЖДЫЙ раздел,
КАЖДУЮ вкладку, КАЖДУЮ подстраницу, КАЖДУЮ функцию.

Затем для КАЖДОЙ найденной вкладки/страницы дай АТОМНЫЙ разбор.

=====================================================
ШАГ 1: DISCOVERY — НАЙДИ ВСЁ
Прежде чем писать отчёт — пройдись по ВСЕМУ коду фронтенда и найди:

ВСЕ страницы/роуты (прочитай роутер, wouter, все routes)
ВСЕ пункты меню для каждой роли (прочитай sidebar-nav, nav-groups)
ВСЕ вкладки внутри каждой страницы (прочитай tab-компоненты)
ВСЕ подстраницы/диалоги (прочитай все компоненты)
ВСЕ кнопки/действия (прочитай все обработчики)
Составь для себя ПОЛНЫЙ ДРЕВОВИДНЫЙ СПИСОК:

Роль → Раздел → Вкладка → Элементы
Не пропускай НИЧЕГО. Если есть вкладка "Foo" внутри раздела "Bar" —
ты должен её найти и описать.

=====================================================
ШАГ 2: АТОМНЫЙ РАЗБОР КАЖДОЙ ВКЛАДКИ
Для КАЖДОЙ вкладки/страницы которую ты нашёл на Шаге 1, опиши:

[Раздел] → [Вкладка/страница]
Путь: (URL)
Доступ: (роль)

Элементы на странице:

[Элемент] — тип (поле/кнопка/таблица/график/селектор/чекбокс)
Для поля: имя, тип, обязательное?, откуда опции (если селект)
Для кнопки: что делает, какой API вызывает
Для таблицы: какие колонки, откуда данные
Для графика: что показывает, откуда данные
[Элемент] — ...
Бэкенд:

Какой API обслуживает (endpoint)
Какая таблица БД
Реальные данные или заглушка/мок/пусто
Вердикт:

Работает? (да/частично/только UI/нет)
Данные реальные? (да/мок/пусто)
Нужна ли функция? (по договору / не по договору / под вопросом)
=====================================================
СТРУКТУРА ОТЧЁТА
Организуй отчёт по ролям:

ADMIN / MANAGER
(сначала перечисли ВСЕ разделы и вкладки что нашёл)
(затем детальный разбор каждой вкладки)

LABEL
(сначала перечисли ВСЕ разделы и вкладки)
(затем детальный разбор)

ARTIST
(сначала перечисли ВСЕ разделы и вкладки)
(затем детальный разбор)

ТОПБАР / ОБЩЕЕ
(профиль, уведомления, кнопки в шапке)

=====================================================
ЧТО ИМЕННО РАЗБИРАТЬ (ДЛЯ КАЖДОЙ ВКЛАДКИ)
КАЖДОЕ поле ввода (имя, тип, обязательность, источник опций)
КАЖДЫЙ селектор/дропдаун (откуда опции — Broma16? локальная таблица?)
КАЖДАЯ кнопка (что делает, какой API, реальный эффект)
КАЖДАЯ таблица (колонки, источник данных, фильтры)
КАЖДЫЙ график/виджет (что показывает, источник данных, реальный/мок)
КАЖДЫЙ чекбокс/переключатель (что контролирует)
КАЖДЫЙ диалог/модалка (когда открывается, что внутри)
=====================================================
ПОТОК ДАННЫХ — ДЛЯ КАЖДОЙ ФУНКЦИИ
Для каждой значимой функции опиши цепочку:

Пользователь нажимает [кнопку]
Фронт вызывает [API endpoint]
Бэк читает/пишет [таблицу]
Возвращает [результат]
Фронт показывает [что]
Особенно для:

Создание релиза (каждый шаг)
Модерация релиза
Отправка в Broma16 (ГДЕ кнопка? КАК админ до неё доходит?)
ACRCloud проверка (ГДЕ кнопка?)
Финансы/выплаты
Publishing
=====================================================
ПОИСК ПРОБЛЕМ
В отдельном разделе выпиши ВСЕ найденные проблемы:

Кнопки/функции не на своём месте
Функции которые есть, но не нужны
Заглушки, моки, фейки
Кнопки без обработчиков
Бэк без фронта (endpoint есть, UI нет)
Фронт без бэка (UI есть, ничего не делает)
Данные захардкожены
Несоответствия между ролями
Страницы которые возвращают ошибку
Для каждой проблемы:

Что это, где (файл/страница)
Почему проблема
Что предлагаешь
=====================================================
ПРАВИЛА
НЕ МЕНЯЙ НИЧЕГО В КОДЕ
НЕ ПРОПУСКАЙ ВКЛАДКИ. Если она существует — опиши её.
Если раздел имеет 7 вкладок — опиши ВСЕ 7, не 4.
Если не уверен сколько вкладок — ПРОЙДИСЬ ПО КОДУ и посчитай.
КАЖДЫЙ элемент, КАЖДАЯ кнопка, КАЖДОЕ поле.
Для каждого элемента — реальные данные или нет.
Не догадывайся — проверяй в коде.
=====================================================
ПРОВЕРКА КАЧЕСТВА
Перед отправкой отчёта, проверь себя:

Я нашёл ВСЕ вкладки в разделе Настройки? (должно быть 15+)
Я нашёл ВСЕ вкладки в разделе Автоматизация? (должно быть 6+)
Я нашёл ВСЕ вкладки в разделе Управление правами? (должно быть 7+)
Я описал КАЖДУЮ вкладку детально, а не просто названием?
Я указал для КАЖДОГО элемента — реальные данные или нет?
Если на какой-то вопрос ответ "нет" — отчёт неполный, доделай.

Этот отчёт критически важен. Отнесись максимально серьёзно.