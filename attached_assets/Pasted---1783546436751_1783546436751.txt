Нам нужен АБСОЛЮТНО ПОЛНЫЙ, ДО МЕЛОЧЕЙ документ всего проекта.
Ты НЕ должен ничего менять в коде. Только АНАЛИЗ и ДОКУМЕНТАЦИЯ.

Это ТРИ ОТДЕЛЬНЫХ АУДИТА в одном документе:

ФРОНТЕНД — каждый раздел, страница, вкладка, кнопка, селектор, поле
БЭКЕНД — каждый API endpoint, каждая таблица, каждая функция
СОПОСТАВЛЕНИЕ — где фронт не подключён к бэку, где заглушки, где фейки
Цель: увидеть ВСЁ и найти заглушки, фейки, недоделки, несоответствия.

=====================================================
ШАБЛОН ДЛЯ ОПИСАНИЯ КАЖДОЙ СТРАНИЦЫ/РАЗДЕЛА
Для каждой страницы описывай по этому шаблону:

[Название раздела/страницы]
Путь: (URL, например /releases или /admin/distribution)
Доступ: (Admin/Manager/Label/Artist/All)

ФРОНТЕНД — что видит пользователь:

[Элемент 1] — тип (кнопка/форма/таблица/график/селектор),
что делает, какие опции
[Элемент 2] — ...
(КАЖДЫЙ элемент, даже мелкие — селекторы, чекбоксы, табы)
Формы (если есть) — КАЖДОЕ поле:

Имя поля
Тип (текст/число/селект/файл/чекбокс)
Откуда опции (если селект — из какого API/таблицы)
Валидация
Обязательное или нет
Кнопки/действия — КАЖДАЯ кнопка:

Что делает
Какой API вызывает
Что происходит после нажатия
БЭКЕНД — что реально работает:

Какие API endpoints обслуживают эту страницу
Какие таблицы БД читаются/пишутся
Реальные ли данные или заглушки/моки/сид
СОПОСТАВЛЕНИЕ:

Фронт подключён к реальному бэку? (ДА/НЕТ/ЧАСТИЧНО)
Есть ли заглушки/моки/фейки? (ГДЕ ИМЕННО?)
Есть ли кнопки которые ничего не делают? (КАКИЕ?)
Есть ли бэк без фронта? (функция работает в API, но в UI нет)
Есть ли фронт без бэка? (UI есть, но ничего не происходит)
Данные реальные или захардкожены?
ОЦЕНКА:

Работает ли реально? (ДА/ЧАСТИЧНО/ТОЛЬКО UI/НЕТ)
Нужна ли эта функция? (по договору / не по договору / под вопросом)
Проблемы (опиши)
=====================================================
РАЗДЕЛ A: ФРОНТЕНД — ADMIN / MANAGER
Пройди по ВСЕМУ что видит Admin и Manager. Не пропусти НИЧЕГО.

Сначала составь ПОЛНЫЙ СПИСОК пунктов меню Admin/Manager.
Затем для КАЖДОГО — детальный разбор по шаблону.

Обязательно:

Dashboard — каждый виджет, KPI, график, фильтр
Catalog — Artists/Tracks/Releases/Labels/Assets
Для каждого: список, карточка, форма (КАЖДОЕ ПОЛЕ!)
Users — список, карточка, заявки, роли, KYC
Distribution / Модерация —
Очередь модерации
Карточка релиза
КАКИЕ кнопки? (ACRCloud? Broma16? Одобрить?)
Где выбираются площадки?
Analytics — графики, виджеты, источники данных
Finance — Royalties/Splits/Payouts
Publishing — произведения, авторы, доли, publishing-only
CRM — контакты, задачи, заметки
Communications — email, шаблоны, кампании
Automation — правила, триггеры (если есть)
Rights Management (если есть)
Marketing (если есть)
Settings — интеграции, общие настройки, словари
Audit Logs
ВСЁ ОСТАЛЬНОЕ — не пропусти ничего!
=====================================================
РАЗДЕЛ B: ФРОНТЕНД — LABEL
Зайди как Label. Опиши ВСЁ что видит.

Dashboard
Releases — создание (КАЖДЫЙ шаг, КАЖДОЕ поле, КАЖДЫЙ селектор!)
⚠️ ВИДИТ ЛИ Label кнопку "Отправить в Broma16"? Отметь!
Где выбирает площадки?
Что после создания?
Publishing
Analytics
Royalties
Marketing (если есть)
Support
Profile / Settings
ВСЁ ОСТАЛЬНОЕ
=====================================================
РАЗДЕЛ C: ФРОНТЕНД — ARTIST
Зайди как Artist. Опиши ВСЁ.

Dashboard
Releases — создание (КАЖДЫЙ шаг, КАЖДОЕ поле!)
⚠️ ВИДИТ ЛИ Artist кнопку "Отправить в Broma16"? Отметь!
Analytics
Royalties
Profile / Settings
ВСЁ ОСТАЛЬНОЕ
=====================================================
РАЗДЕЛ D: СОЗДАНИЕ РЕЛИЗА — АТОМНЫЙ РАЗБОР
Разбери создание релиза ДО АТОМОВ. Для каждой роли.

ОПИШИ КАЖДЫЙ ЭТАП/ШАГ:

Какие поля (имя, тип, опции, валидация, обязательность)
Какие селекторы (откуда опции — из Broma16? из локальной таблицы?)
Какие кнопки
Что происходит при сохранении/переходе
Куда уходят данные (какой API, какая таблица)
ПОТОМ — что происходит ПОСЛЕ создания:

Статус релиза
Кто что видит
Где админ модерирует
Где кнопка ACRCloud (в каком разделе, на какой странице)
Где кнопка Broma16 push
Где кнопка проверки статуса
Где выбор площадок
Правильный ли это flow? (должен ли артист видеть Broma16?)
=====================================================
РАЗДЕЛ E: БЭКЕНД — ПОЛНЫЙ АУДИТ API
Перечисли ВСЕ API endpoints в проекте. Для каждого:

Метод + путь (GET/POST/PUT/DELETE /api/...)
Назначение
Какие таблицы читает/пишет
Реальные данные или заглушки
Подключён ли к фронту (какой компонент вызывает)
Доступ (какие роли)
Раздели endpoints по группам:

Auth
Releases
Tracks
Artists
Labels
Users
Distribution / Moderation
Broma16 (push, statistics, dictionaries, moderation status)
ACRCloud (drop, verdicts)
Analytics
Finance (splits, payouts, transactions)
Publishing
CRM
Communications / Email
Settings / Integrations
Automation
Audit logs
ВСЁ ОСТАЛЬНОЕ
=====================================================
РАЗДЕЛ F: БЭКЕНД — ТАБЛИЦЫ БД
Перечисли ВСЕ таблицы БД. Для каждой:

Имя таблицы
Кол-во строк (реальных, не сид)
Назначение
Кто пишет (какой API/процесс)
Кто читает (какой API/компонент)
Есть ли в таблице данные? (или пустая)
Особо отметь:

Пустые таблицы (0 строк)
Таблицы только с сид-данными
Таблицы с реальными данными
=====================================================
РАЗДЕЛ G: СОПОСТАВЛЕНИЕ ФРОНТА И БЭКА
ЭТО САМАЯ ВАЖНАЯ ЧАСТЬ.

Найди и опиши ВСЕ несоответствия между фронтом и бэком:

G1. Фронт есть, бэк — заглушка/мок
Список страниц/функций где UI показывает что-то,
но данные НЕ реальные (моки, захардкожено, сид).

G2. Фронт есть, бэка нет
Список кнопок/форм/страниц которые есть в UI,
но НЕ вызывают ничего реального (или вызывают несуществующий API).

G3. Бэк есть, фронта нет
Список API endpoints которые работают, но в UI нет
ни одной кнопки/страницы которая их вызывает.

G4. Данные фейковые
Где конкретно данные захардкожены или моковые?
(например, "график стримов показывает demo данные")

G5. Частичная реализация
Где фронт вызывает бэк, но бэк делает не всё?
(например, "форма сохраняет, но не отправляет в Broma16")

=====================================================
РАЗДЕЛ H: ПОТОК BROMA16 И ACRCLOUD — ГДЕ ЧТО
Ответь детально:

Где В UI находится кнопка "Отправить в Broma16"?
(в форме создания релиза? в модерации? в карточке?)
Где В UI находится кнопка ACRCloud?
Где В UI находится выбор площадок/витрин?
Где В UI находится кнопка проверки статуса модерации?
Может ли артист/лейбл увидеть кнопку Broma16? (должен ли?)
Может ли артист/лейбл отправить в Broma16 напрямую?
Как админ доходит до кнопок отправки? (опиши путь кликов)
Какие именно данные отправляются в Broma16 на каждом из 9 шагов?
(не просто "метаданные", а КОНКРЕТНО какие поля)
=====================================================
РАЗДЕЛ I: ПРОБЛЕМЫ И НЕСОСТЫКОВКИ
В конце ОТДЕЛЬНО выпиши:

I1. Функции не на своём месте
(как кнопка Broma16 в создании релиза)

I2. Функции не нужные / не по договору
(видео, фрод, маркетинг — что есть, что работает)

I3. Недоделанные функции
(есть UI но бэк не доделан, или наоборот)

I4. Несоответствия между ролями
(артист видит то что не должен, или не видит то что должен)

I5. Нелогичные flow
(последовательность действий не имеет смысла)

I6. Заглушки и фейки
(где конкретно данные ненастоящие)

Для каждой проблемы:

Что это
Где находится (файл, страница, компонент)
Почему проблема
Что предлагаешь (скрыть/переместить/доделать/оставить)
=====================================================
ПРАВИЛА
НЕ МЕНЯЙ НИЧЕГО В КОДЕ. Только анализ.
БУДЬ МАКСИМАЛЬНО ДЕТАЛЬНЫМ. Лучше больше, чем меньше.
Проверяй в коде, не догадывайся.
Опиши КАЖДУЮ кнопку, КАЖДОЕ поле, КАЖДЫЙ селектор.
Для каждого элемента скажи: реальные данные или нет.
Не пропускай "мелочи".
Структурируй по разделам A-I.
В конце дай ОБЩУЮ КАРТУ меню по ролям.
Этот отчёт — самый важный документ для нас.
Отнесись максимально серьёзно. От него зависит сдача проекта.